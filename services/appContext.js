import { createContext } from "react";

/**
@author Liam Strang - 45426392
@description Creates the context for the app which passes state down to the components
*/
export const AppContext = createContext(null);
